# Status — Business Intelligence

## Current State

Phase 2 — Chart Coverage. **193 of 193** chart types implemented. The foundation, the product differentiator, three breadth waves, Gate 3, the first twenty-eight M3 wave-4 tranches, the first thirteen M4 analysis-loop/data-quality/validation/cleanup/export/keyboard slices, and the first six M5 renderer-infrastructure/geographic/3D slices are in place (M0 + M1 + M2 + M3 waves 1–3 + M3 wave 4 tranches 1–28 + M4 slices 1–13 + M5 slices 1–6 of [`docs/EXECUTION_ROADMAP.md`](EXECUTION_ROADMAP.md)). The final catalog-alignment pass moved `qq_plot` into Distribution and added Statistical's `confusion_matrix_chart`, bringing every family to its catalog target. Post-completion polish has also closed the remaining semantic shape-detection gap, landed a live regl base renderer/wrapper, migrated `gauge` to Canvas2D as the first Canvas2D catalog chart, migrated `image_raster_plot` to regl as the first regl catalog chart, added Excel workbook import for `.xlsx`/`.xlsm`, and added client-side Parquet import for `.parquet`.

- **M0 (shipped):** Tailwind v4's Vite plugin is wired (app renders fully styled); chart families load deterministically before first paint (the 500 ms `setTimeout` race is gone).
- **M1 (shipped):** declarative `ChartOptionSpec` options schema + generic `ChartOptionsPanel` (dead scatter/line controls fixed); shared ECharts helpers (`buildCartesianAxes`/`buildGrid`/`buildTooltip`); empty-data guard (`EmptyChartState`); `categoricalColor`; name-aware, consume-on-assign column auto-assignment + a "no compatible column" state; a per-chart **contract test harness** + a CI guard against backends with no base class.
- **M2 (shipped):** the **shape→ranked-suggestion differentiator is wired** — `ChartPicker` shows a "Suggested for <shape>" section ranked by a new `scoreChart(def, shape, columns)` (shape-compatible + column-fillable, most-specialized first), with a "Show all charts" escape hatch; `defaultSuggestions` dead code deleted. Shape detector: `category_numeric` relaxed to any `category + ≥1 numeric`, `matrix` detection added (`row+col+value`), `survival`/`event_log`/`geo_polygons` detection added, the `'at'` substring datetime bug fixed, thresholds named.

- **M3 wave 1 (shipped):** 12 ECharts-native charts via a fan-out workflow — categorical `bar`/`horizontal_bar`/`grouped_bar`/`stacked_bar`, composition `pie`/`donut`, time-series `area`/`stacked_area`/`multi_line`/`step`, relationships `bubble`, matrix `heatmap`. Extracted shared `pivotLongForm` + `finiteCategoryValues` (root-caused from a 31-agent review's two high-severity pivot bugs).
- **M3 wave 2 (shipped):** a reference-validated stats layer (`src/data/stats/`: `quantiles`, `kernelDensity`, `buildHierarchy`) + 10 charts via a multi-phase workflow — distribution `box_plot`/`ecdf`/`kde`, finance `candlestick`/`volume`, network-flow `sankey`/`funnel`, hierarchical `treemap`/`sunburst`, categorical `lollipop`. All 5 family barrels wired.
- **M3 wave 3 (shipped):** the statistical family + everyday remainder — 18 charts on a new reference-validated compute layer (`rocCurve`, `prCurve`, `calibrationCurve`, `normalQuantile`/`qqPoints`, `histogramBins`, `groupByAggregate`). Statistical `error_bar`/`residual_plot`/`actual_vs_predicted`/`roc_curve`/`pr_curve`/`calibration_curve`/`qq_plot`/`feature_importance`; distribution `frequency_polygon`; categorical `pareto`/`percent_stacked_bar`; time-series `calendar_heatmap`/`streamgraph`; matrix `annotated_heatmap`; hierarchical `tree`; network-flow `force_directed_graph`; finance `ohlc`; specialized `gauge`. `bar`/`horizontal_bar`/`pie`/`donut`/`lollipop`/`pareto` aggregate by category via `groupByAggregate`/`aggregatedCategoryValues` (fixes the wave-1 "one bar/slice per raw row" bug + drops non-finite).
- **M3 wave 4 tranche 1 (shipped):** finance breadth + Gate-3 cleanup — `heikin_ashi`, `price_volume`, `drawdown`, `equity_curve`, and `return_series_line`; the prior Gate-3 `gauge` detail precision and legend-overlap cosmetics are fixed; `run_e2e.bat` is now verified on Windows.
- **M3 wave 4 tranche 2 (shipped):** categorical breadth — `cleveland_dot`, `dumbbell`, `slope`, and `waffle`, all ECharts-native, registered through the categorical barrel, covered by focused unit tests and Gate-3 sample mappings.
- **M3 wave 4 tranche 3 (shipped):** statistical/model-evaluation breadth — `mean_ci_band`, `forest_plot`, `bland_altman`, `lift_chart`, and `gain_chart`, all ECharts-native, wired through the statistical barrel, covered by focused unit tests and Gate-3 sample mappings.
- **M3 wave 4 tranche 4 (shipped):** relationships breadth — `colored_scatter`, `regression_plot`, `polar_scatter`, `polar_line`, and `ternary`, all ECharts-native, wired through the relationships barrel, covered by focused unit tests, suggestion coverage, and Gate-3 sample mappings.
- **M3 wave 4 tranche 5 (shipped):** distribution breadth — `rug_plot`, `strip_plot`, `dot_plot`, `violin_plot`, and `ridgeline_plot`, all ECharts-native, wired through the distribution barrel, covered by focused unit tests, suggestion coverage, and Gate-3 sample mappings.
- **M3 wave 4 tranche 6 (shipped):** time-series breadth — `percent_stacked_area`, `sparkline`, `run_chart`, `control_chart`, and `fan_chart`, all ECharts-native, wired through the time-series barrel, covered by focused unit tests, suggestion coverage, deterministic samples, and Gate-3 sample mappings.
- **M3 wave 4 tranche 7 (shipped):** categorical completion — `mosaic_plot`, `marimekko`, `pictogram`, and `spine_plot`, all ECharts-native, wired through the categorical barrel, covered by focused unit tests, suggestion coverage, and Gate-3 sample mappings. The categorical family is now **15/15**.
- **M3 wave 4 tranche 8 (shipped):** distribution breadth — `beeswarm`, `joy_plot`, `pp_plot`, `probability_plot`, and `cumulative_distribution_plot`, all ECharts-native, wired through the distribution barrel, covered by focused unit tests, suggestion coverage, and Gate-3 sample mappings. The distribution family is now **15/21**.
- **M3 wave 4 tranche 9 (shipped):** distribution breadth — `stem_and_leaf`, `raincloud_plot`, `sina_plot`, `letter_value_plot`, and `quantile_dot_plot`, all ECharts-native, wired through the distribution barrel, covered by focused unit tests, suggestion coverage, and Gate-3 sample mappings.
- **M3 wave 4 tranche 10 (shipped):** time-series breadth — `horizon_chart`, `seasonal_subseries_plot`, `lag_plot`, `autocorrelation_plot`, and `forecast_cone`, all ECharts-native, wired through the time-series barrel, covered by focused unit tests, suggestion coverage, a reference-tested `autocorrelation` helper, deterministic sample mappings, and Gate-3 baselines. The time-series family is now **17/22**.
- **M3 wave 4 tranche 11 (shipped):** matrix/grid breadth — `clustermap`, `confusion_matrix`, `calendar_matrix`, `tile_map`, and `quilt_plot`, all ECharts-native, wired through the matrix barrel, covered by focused unit tests, suggestion coverage, shared `matrixGrid` helper coverage, deterministic sample mappings, and Gate-3 baselines. The matrix family is now **7/10**.
- **M3 wave 4 tranche 12 (shipped):** specialized breadth — `kpi_card`, `bullet_chart`, `funnel_area`, `pyramid_chart`, and `population_pyramid`, all ECharts-native, wired through the specialized barrel, covered by focused unit tests, suggestion coverage, deterministic `kpi`/`demographics` samples, and Gate-3 baselines. The specialized family is now **6/17**.
- **M3 wave 4 tranche 13 (shipped):** time-series completion — `partial_autocorrelation_plot`, `event_timeline`, `gantt_chart`, `swimlane_timeline`, and `range_bar`, all ECharts-native, wired through the time-series barrel, covered by focused unit tests, suggestion coverage, a reference-tested `partialAutocorrelation` helper, deterministic `timeline` sample data, and Gate-3 baselines. The time-series family is now **22/22 complete**.
- **M3 wave 4 tranche 14 (shipped):** specialized analytics breadth — `topic_term_bubble`, `cohort_retention_heatmap`, `retention_curve`, `conversion_path_chart`, and `bump_chart`, all ECharts-native, wired through the specialized barrel, covered by focused unit tests, suggestion coverage, deterministic `topics`/`cohort`/`conversionPath`/`ranking` sample data, and Gate-3 baselines. The specialized family is now **11/17**.
- **M3 wave 4 tranche 15 (shipped):** finance/statistical breadth — `rolling_volatility_plot`, `order_book_depth_chart`, `yield_curve`, `candlestick_heatmap_by_hour_day`, `survival_curve`, and `cumulative_hazard_plot`, all ECharts-native, wired through the finance/statistical barrels, covered by focused unit tests, suggestion coverage, deterministic `orderBook`/`yieldCurve`/`tradingBuckets`/`survival` sample data, a reference-tested `survivalCurve` helper, and Gate-3 baselines. Finance is now **12/15** and statistical is now **15/19**.
- **M3 wave 4 tranche 16 (shipped):** relationships breadth — `loess_smoother_plot`, `faceted_scatter`, `pca_scatter`, `t_sne_plot`, `umap_plot`, and `radar`, all ECharts-native, wired through the relationships barrel, covered by focused unit tests, suggestion coverage, deterministic `embedding` sample data, and Gate-3 baselines. Relationships is now **13/26**.
- **M3 wave 4 tranche 17 (shipped):** hierarchical completion — `icicle`, `dendrogram`, `radial_tree`, `circle_packing`, and `partition_chart`, all ECharts-native, wired through the hierarchical barrel, covered by focused unit tests, suggestion coverage, deterministic hierarchy sample mappings, and Gate-3 baselines. The hierarchical family is now **8/8 complete**.
- **M3 wave 4 tranche 18 (shipped):** network-flow/composition breadth — `arc_diagram`, `chord_diagram`, `alluvial_diagram`, `waterfall_chart`, and `nested_donut`, all ECharts-native, wired through the network-flow/composition barrels, covered by focused unit tests, suggestion coverage, a deterministic `journey` sample, and Gate-3 baselines. Network-flow is now **7/10** and composition is now **3/10**.
- **M3 wave 4 tranche 19 (shipped):** finance completion — `renko`, `kagi`, and `point_and_figure`, all ECharts-native, wired through the finance barrel, covered by focused unit tests, suggestion coverage, and Gate-3 sample mappings/baselines. The finance family is now **15/15 complete**.
- **M3 wave 4 tranche 20 (shipped):** statistical/model-explainability completion — `shap_summary_beeswarm`, `shap_dependence_plot`, `partial_dependence_plot`, and `ice_plot`, all ECharts-native, wired through the statistical barrel, covered by focused unit tests, suggestion coverage, a deterministic `explainability` sample, and Gate-3 baselines. The statistical family is now **19/19 complete**.
- **M3 wave 4 tranche 21 (shipped):** matrix completion — `correlation_matrix`, `distance_matrix_heatmap`, and `image_raster_plot` originally shipped as matrix charts; `correlation_matrix` and `distance_matrix_heatmap` remain ECharts-native, while `image_raster_plot` now renders through regl. They are wired through the matrix barrel, covered by focused unit tests, suggestion coverage, existing deterministic `matrix` sample mappings, and Gate-3 baselines. The matrix family is now **10/10 complete**.
- **M3 wave 4 tranche 22 (shipped):** specialized breadth — `word_cloud`, `sequence_diagram`, and `ranking_table_with_sparklines`, all ECharts-native, wired through the specialized barrel, covered by focused unit tests, suggestion coverage, deterministic `topics`/`sequence`/`ranking` sample mappings, and Gate-3 baselines. The specialized family is now **14/17**.
- **M3 wave 4 tranche 23 (shipped):** network-flow completion — `network_graph`, `dependency_graph`, and `adjacency_matrix`, all ECharts-native, wired through the network-flow barrel, covered by focused unit tests, suggestion coverage, deterministic `flow` sample mappings, and Gate-3 baselines. The network-flow family is now **10/10 complete**.
- **M3 wave 4 tranche 24 (shipped):** specialized completion — `table`, `small_multiples`, and `faceted_dashboard_grid`, all ECharts-native, wired through the specialized barrel, covered by focused unit tests, suggestion coverage, deterministic `sales` sample mappings, and Gate-3 baselines. The specialized family is now **17/17 complete**.
- **M3 wave 4 tranche 25 (shipped):** relationships density/contour breadth — `joint_plot`, `hexbin_plot`, `two_d_density_plot`, `contour_plot`, and `filled_contour`, all ECharts-native, wired through the relationships barrel, covered by focused unit tests, suggestion coverage, deterministic `numeric` sample mappings, and Gate-3 baselines. Added shared `relationshipGrid` helpers for finite x/y(/z) extraction and binned density/mean grids. Relationships is now **18/26**.
- **M3 wave 4 tranche 26 (shipped):** relationships multivariate breadth — `biplot`, `andrews_curves`, `radviz`, `parallel_coordinates`, `scatter_matrix`, `correlation_heatmap`, and `covariance_heatmap`, all ECharts-native, wired through the relationships barrel, covered by focused unit tests, suggestion coverage, deterministic `numeric`/`embedding` sample mappings, and Gate-3 baselines. Added shared `multivariate` helpers for finite feature rows, extents, association matrices, and RadViz projection. Relationships is now **25/26**.
- **M3 wave 4 tranche 27 (shipped):** composition completion — `composition_stacked_bar`, `composition_percent_stacked_bar`, `composition_stacked_area`, `composition_percent_stacked_area`, `composition_treemap`, `composition_sunburst`, and `composition_waffle`, all ECharts-native, wired through the composition barrel, covered by focused unit tests, suggestion coverage, deterministic `sales`/`stock`/`hierarchy` sample mappings, and Gate-3 baselines. These use composition-prefixed registry keys for catalog concepts that overlap categorical/time-series/hierarchical chart names. The composition family is now **10/10 complete**.
- **M3 wave 4 tranche 28 (shipped):** relationship completion — `pair_plot`, an ECharts-native pairwise multivariate chart with histogram diagonals and scatter off-diagonal cells, wired through the relationships barrel, covered by focused unit tests, suggestion coverage, deterministic `numeric` sample mapping, and a Gate-3 baseline. The relationships family is now **26/26 complete**.
- **M4 analysis-loop slice 1 (shipped):** the Data tab now has a row-preview table, multi-dataset switcher, and reachable filter authoring/toggle/remove/clear controls; the Layers tab can activate layers; the samples menu uses Radix dropdown behavior; and theme choice persists through `localStorage`.
- **M4 analysis-loop slice 2 (shipped):** `ChartArea` now renders compatible optional column pickers and persists/removes optional role assignments, UI-created filters are integration-tested through the full Data tab → `applyFilters` → renderer path, and invalid regex filters fail closed instead of crashing render.
- **M4 data-quality slice 3 (shipped):** CSV/JSON-loaded rows now normalize formatted numeric strings before column analysis and column-array indexing, including currency, thousands separators, signed/accounting negatives, and percentages, while preserving code-like leading-zero strings.
- **M4 validation slice 4 (shipped):** `resolveOptions` now validates `select` values against declared choices, falling back to defaults for stale/imported invalid values while keeping choice-less select specs permissive.
- **M4 token-cleanup slice 5 (shipped):** pre-existing hard-coded white foregrounds in the upload and error-boundary action buttons now use theme variables, and `mosaic_plot` no longer builds its empty-state check with a fake literal-color theme.
- **M4 store-cleanup slice 6 (shipped):** all five Zustand stores now use the documented `immer` middleware for draft-based updates, including enabled Map/Set support for the dataset store.
- **M4 dependency-cleanup slice 7 (shipped):** `npm audit` is clean (0 vulnerabilities) after upgrading Vitest/coverage to 4.1.8, Vite to 8.0.16, the Vite plugins to current compatible minors, and deck.gl packages to 9.3.3; deck.gl test mocks were updated for the 9.3 ref contract.
- **M4 data-quality slice 8 (shipped):** import normalization now handles locale-style numeric punctuation (`1.234,50`, `1 234,50`, Swiss apostrophe grouping, comma decimals, localized percentages) and normalizes clear date-like column values to ISO dates for unambiguous slash/dot/month-name formats while preserving ambiguous dates and non-date text columns.
- **M4 export slice 9 (shipped):** the Data tab now exposes filtered CSV export and chart-spec JSON export. CSV export uses the same `applyFilters` data view as rendering; spec export captures dataset metadata, active layer config, options, and filters.
- **M4 annotation slice 10 (shipped):** the Data tab now exposes dataset-scoped row annotations with add/remove/clear controls. Chart-spec JSON export includes active-dataset annotations with stable ISO timestamps.
- **M4 image-export slice 11 (shipped):** the chart header now exposes active chart PNG export. Export is scoped to the active chart render root, serializes the first non-empty canvas as `image/png`, and downloads through a data-URL helper.
- **M4 image-export slice 12 (shipped):** the chart header now exposes SVG export where a renderer emits SVG. Export is scoped to the active chart render root, serializes the first SVG node with `XMLSerializer`, and downloads it as an encoded `image/svg+xml` data URL.
- **M4 keyboard slice 13 (shipped):** a command palette is reachable from the toolbar and `Ctrl/Cmd+K`, closes on Escape/backdrop, and runs existing app actions: switch sidebar tabs, toggle theme, and load sample datasets.
- **M5 renderer-infrastructure slice 1 (shipped):** Canvas2D is now a live backend. `Canvas2DBaseRenderer` mirrors the other renderer bases, and `Canvas2DChart` owns a dedicated canvas with device-pixel-ratio scaling, resize redraw, empty-state handling through the base class, missing-context safety, and optional renderer cleanup on unmount. The registry contract accepts Canvas2D renderers only when they extend the live base class.
- **M5 renderer-infrastructure slice 2 (shipped):** deck.gl now has a small view abstraction. `DeckGLBaseRenderer` can select Map/Orbit/Orthographic views and provide data-driven initial view state; `point_map` is the first real deck.gl catalog chart, rendering latitude/longitude rows through `ScatterplotLayer` with theme-token colors and a bounds-derived MapView state.
- **M5 geographic slice 3 (shipped):** `bubble_map`, `symbol_map`, and `route_map` are now registered deck.gl charts. Shared `geo-utils` handles finite coordinate extraction, bounds-derived view state, theme-token color conversion, radius scaling, and palette selection; the deterministic geo sample now includes `value`, `category`, and `order` roles for name-aware auto-assignment.
- **M5 geographic slice 4 (shipped):** `density_map`, `hexbin_map`, `geospatial_heatmap`, and `voronoi_map` are now registered deck.gl charts. The first three use deck.gl aggregation layers for screen-grid density, weighted hexagonal bins, and weighted heatmap rendering; `voronoi_map` uses deterministic local geographic cells through `PolygonLayer` for a client-only spatial-partition view.
- **M5 geographic slice 5 (shipped):** `choropleth_map`, `filled_map`, `cartogram`, `flow_map`, `isochrone_contour_map`, and `tile_grid_map` are now registered deck.gl charts. Geographic is now complete at **14/14** with point, aggregation, region, flow, route, contour, partition, and tile-grid views.
- **M5 3D slice 6 (shipped):** all six 3D catalog charts are now registered deck.gl OrbitView charts: `three_d_scatter`, `three_d_surface`, `three_d_wireframe`, `three_d_contour`, `three_d_bar_chart`, and `three_d_volume_rendering`.
- **Phase 2 catalog completion (shipped):** `qq_plot` now registers from the Distribution family, and `confusion_matrix_chart` fills the Statistical catalog's classification confusion-matrix slot. All 13 families are now complete and `/chart-status` reports **193/193**.
- **Post-completion shape alignment (shipped locally):** `detectShape` now emits the remaining documented semantic shapes: `geo_polygons` for explicit geometry or simple `region,value` tables, `survival` for time-to-event data, and `event_log` for user/event/timestamp data. `event_timeline` now targets `event_log`, and role-name aliases auto-assign common event-log columns such as `timestamp` and `event`.
- **Post-completion regl base (shipped locally):** `ReglBaseRenderer` and `ReglChart` make `regl` a live renderer backend. The wrapper owns a dedicated canvas, WebGL context/regl instance creation, device-pixel-ratio sizing, resize redraw, missing-context safety, empty-state handling through the base class, optional renderer cleanup, and regl destruction on unmount. The registry contract now accepts regl charts only when they extend `ReglBaseRenderer`.
- **Post-completion Canvas2D chart (shipped locally):** `gauge` now renders through `Canvas2DBaseRenderer`/`Canvas2DChart`, making Canvas2D a proven catalog backend. The deterministic Gate-3 baseline was regenerated for `gauge`, targeted Docker update/check passed, and the full pinned Docker Playwright check passed **193/193** charts.
- **Post-completion regl chart (shipped locally):** `image_raster_plot` now renders through `ReglBaseRenderer`/`ReglChart`, making regl a proven catalog backend. It draws finite raster cells as themed shader rectangles with one dedicated WebGL canvas, preserves the existing row/column/intensity contract and empty-state behavior, and has a regenerated Gate-3 baseline. The full pinned Docker Playwright check passed **193/193** charts after the migration.
- **Post-completion file-format slice (shipped locally):** Excel `.xlsx` and `.xlsm` workbooks now route through `read-excel-file` into the same `DataSet` pipeline as CSV/JSON. The parser treats the first non-empty sheet row as headers, skips blank data rows, fills missing trailing cells with `null`, normalizes Date cells to ISO strings before the existing import-normalization pass, and preserves downstream shape detection/column arrays.
- **Post-completion Parquet file-format slice (shipped locally):** Parquet `.parquet` files now route through `hyparquet` into the same `DataSet` pipeline as CSV/JSON/Excel. The parser reads row objects, unions sparse row keys into stable first-seen column order, normalizes Date values to ISO strings, converts safe bigint values to numbers, preserves unsafe bigint values as strings, and preserves downstream shape detection/column arrays.
- **Post-completion bundle split (shipped locally):** Vite/Rolldown now groups stable vendor chunks (`react`, ECharts/zrender, deck.gl/luma/loaders, D3, Radix/lucide, and file-format parsers) separately from the app entry and chart-family chunks. The production app entry dropped from ~1.68 MB to ~163 kB minified; the residual 500 kB warning is now isolated to ECharts/deck.gl vendor chunks.
- **Post-completion upload affordance polish (shipped locally):** the toolbar upload input now advertises every supported frontend import format (`.csv`, `.tsv`, `.json`, `.xlsx`, `.xlsm`, `.parquet`) instead of hiding newly supported Excel/Parquet files from the file picker.
- **Post-completion responsive layout polish (shipped locally):** the app workspace now stacks the sidebar above the chart on narrow viewports, keeps the sidebar scrollable with a bounded mobile height, and lets the chart header controls scroll horizontally instead of forcing page overflow. Playwright layout metrics were checked at 1280×800 and 390×844 with no page overflow and a visible chart render area.
- **Post-completion filter UX polish (shipped locally):** the Data tab now shows the active filtered row count (`Showing N of M rows`) next to the filter authoring controls, reusing the same filtered `DataView` path as chart rendering and CSV export.
- **Post-completion Windows launcher verification (shipped locally):** `bi_service.bat` was verified on Windows with `BI_PORT=5182`; it built the Docker image, started the nginx container, printed the service URL/menu, accepted the `k` stop option, and stopped/removed the container/network while keeping the image.
- **Post-completion backend catalog annotation (shipped locally):** `CHARTS.md` now includes a machine-checkable renderer backend catalog for all 193 registry type IDs. `tests/unit/charts/charts-doc-backends.test.ts` fails if any chart is missing, duplicated, stale, or documented with the wrong renderer backend.
- **Post-completion visual-gate hardening (shipped locally):** the pinned Playwright visual gate now runs with conservative worker defaults (`E2E_WORKERS=1` in CI/Docker wrappers, overrideable locally), a 60s per-test timeout, and a 15s assertion timeout. This prevents the preview server memory pressure and slow first-screenshot retries that can make later chart cases fail with `ERR_CONNECTION_REFUSED`, while preserving the 193-chart non-blank + screenshot gate.
- **Post-completion deck.gl switch-stress coverage (shipped locally):** `tests/e2e/deckgl-switch-stress.spec.ts` drives repeated real-browser switches across deck.gl MapView and OrbitView charts, including dataset changes, and fails on page errors, unfilled role state, or missing canvases. This covers the previously deferred real-GL repeated chart-change cleanup path.
- **Post-completion sidebar structure cleanup (shipped locally):** `Sidebar.tsx` is now only the tab host; `DataTab.tsx` owns dataset metadata, preview, filters, annotations, and data/spec export UI; `LayersTab.tsx` owns layer activation/removal UI. This closes the documented one-concept-per-file debt for the sidebar without changing behavior.
- **Post-completion documentation debt closeout (shipped locally):** the roadmap and project guide now reflect the already-shipped UI color cleanup and `src/lib` formatter/palette split, so the remaining roadmap debt list no longer points at files or literals that do not exist in source.
- **Gate 3 — visual-regression harness (shipped; green):** a Playwright suite (`tests/e2e/charts.spec.ts`) drives the **real UI** (load a deterministic sample → "Show all charts" → click the chart → wait for auto-assign) and asserts, for all 193 charts, BOTH a **non-blank canvas** (reads pixels — the thing jsdom's mocked canvas can't) AND a **screenshot baseline** (`tests/e2e/__screenshots__/*.png`, 192 baselines). Runs in the pinned `mcr.microsoft.com/playwright:v1.60.0-noble` image so baselines are deterministic and match CI. Supporting pieces: `playwright.config.ts`; `run_e2e.{sh,bat}` (check / `update` modes); a `Visual Regression (Playwright)` CI job; 29 deterministic sample datasets (`src/data/sample-data.ts`) + Toolbar wiring; `data-sample`/`data-chart-type`/`data-testid="chart-render"` hooks; a vitest **drift guard** (`tests/unit/charts/e2e-coverage.test.ts`) that fails if any registered chart lacks a visual-gate entry; `window.__E2E__` flag (`echarts-renderer.tsx`) disabling ECharts animation for deterministic frames. `force_directed_graph` is floor-only (no screenshot) because its layout is `Math.random()`-seeded. A 25-agent adversarial review of the harness landed 13 fixes (incl. tied-score determinism, treemap/sunburst zero-value-root collapse, the WebGL-safe non-blank check).

**Current point / handoff:** Gate 3 is mapped for all 193 registered charts, runnable via `./run_e2e.sh` or `run_e2e.bat` and in CI. Every catalog family is complete: distribution 21/21, categorical 15/15, time-series 22/22, relationships 26/26, matrix 10/10, hierarchical 8/8, network-flow 10/10, geographic 14/14, finance 15/15, statistical 19/19, composition 10/10, specialized 17/17, and 3D 6/6. M4 slices 1–13 are shipped, plus local Excel `.xlsx`/`.xlsm` and Parquet `.parquet` import support. M5 slices 1–6 are shipped: Canvas2D now has a tested base renderer and `gauge` catalog chart, regl has a tested base renderer and `image_raster_plot` catalog chart, deck.gl has view selection, all fourteen geographic catalog charts are live, and all six 3D catalog charts are live. Phase 2 is formally closed out on `main` at `125061d5`: GitHub Actions run #5 passed lint, typecheck, test+coverage, build, Docker build, and the 193-chart Playwright visual-regression gate.

Historical M5 3D slice 6 verification: focused 3D/registry/sample/e2e-coverage tests clean, full suite at 100% coverage, local build gates green, and the pinned Docker Playwright update/check passed for that slice before the final catalog-alignment pass.

Empirically verified for Phase 2 catalog completion: focused QQ/confusion-matrix/registry/e2e-coverage/suggestion tests clean (**49 tests**), full suite green (**1463 Vitest tests / 100% coverage**), `npm run typecheck`, `npx tsc --noEmit`, `npm run lint`, `npm run build`, and `npm audit` all green. Chart status is **193/193** with every family at target. The pinned Docker Playwright update pass wrote the `confusion-matrix-chart` baseline and passed **193/193** charts with `--workers=4`; the follow-up Docker check passed **193/193** charts with `--workers=4`.

- **Post-completion layer visibility wiring (shipped locally):** the Layers tab now exposes each layer's existing `visible` state with hide/show controls, and `ChartArea` respects hidden active layers by unmounting the chart renderer and showing a hidden-layer state. Full multi-layer compositing shipped later; shared scale merging remains future Phase 3 work.
- **Post-completion basic layer compositing (shipped locally):** `ChartArea` now mounts every visible, fillable layer into the same chart render root so users can compose multiple chart layers without switching away from the active layer controls. Each layer still owns its own renderer/canvas; shared-axis option merging remains future Phase 3 work.
- **Post-completion composed PNG export (shipped locally):** chart PNG export now composites multiple canvas-backed layers in DOM order instead of serializing only the first canvas. Single-canvas exports keep the direct fast path; SVG export still serializes the first SVG node.
- **Post-completion chart-spec layer-stack export (shipped locally):** chart-spec JSON export now includes the complete layer stack and `activeLayerIndex` while retaining the legacy `activeLayer` field, so composed chart image export and chart-spec export describe the same layer state.
- **Post-completion layer axis assignment (shipped locally):** the Layers tab now exposes each layer's `axis` as a `y1`/`y2` segmented control, and composed chart layer nodes carry `data-layer-axis` for export/diagnostic paths. Shared ECharts scale merging is still future work because each layer owns its renderer canvas.

## What's Built

- **Chart Registry**: `ChartRegistry` singleton with register/get/getByFamily/suggestForShape/all/families/count, duplicate-register throws
- **Renderer Architecture**: 4 live backends (ECharts, deck.gl with `DeckGLChart` FC + Map/Orbit/Orthographic view selection + `finalize()` on unmount, Canvas2D with `Canvas2DChart` FC + DPR-scaled redraw/cleanup, and regl with `ReglChart` FC + DPR-scaled redraw/cleanup)
- **Data Pipeline**: file upload → PapaParse CSV/TSV, `read-excel-file` XLSX/XLSM, `hyparquet` Parquet, or JSON → formatted numeric + locale/date normalization → `analyzeColumns` → `detectShape` → `suggestCharts`
- **State Management**: Zustand stores — dataset, chart, ui, filter, annotation — with `immer` middleware for draft-based updates
- **Analysis UI / data quality / validation / cleanup / export / keyboard (M4 slices 1–13)**: Data tab preview table, active-dataset switcher, filter authoring/toggle/remove/clear controls with verified chart coupling; dataset-scoped row annotations; filtered CSV and chart-spec JSON export; active chart PNG export; SVG export where supported; command palette with keyboard shortcut; optional column role pickers; formatted numeric and locale/date import normalization; chart option select membership validation; theme-token cleanup; Zustand immer adoption; npm audit cleanup; Layers tab activation; Radix-backed samples menu; persisted theme choice.
- **Theming**: dark/light tokens through `ThemeProvider` (`theme-provider.tsx`) + `useTheme` hook (`theme-context.ts`), with `localStorage` persistence under `bi-theme`
- **Testing**: Vitest 4 + @testing-library/react + jsdom. **1495 tests, 100% stmts / 100% branches / 100% funcs / 100% lines** on `src/**` (excluding `main.tsx`, `App.tsx`, `src/types/**`, `src/**/index.ts` barrels, `*.css`). Includes a registry contract harness that validates every registered chart.
- **Visual gate (Gate 3)**: Playwright `tests/e2e/charts.spec.ts` renders all 193 charts in real Chromium (non-blank floor + screenshot baselines), run in Docker via `run_e2e.{sh,bat}` and a CI job. Baselines live in `tests/e2e/__screenshots__/` (192 screenshot baselines plus the non-deterministic `force_directed_graph` floor-only check); the chart→sample map is `tests/e2e/chart-samples.ts`. (NOT part of vitest coverage; separate from the 1495 unit tests.)
- **CI**: `.github/workflows/ci.yml` — lint → **sast** → typecheck → test+coverage → build → **visual (Playwright)** → docker (staging/main only). The `sast` job (CLAUDE.md §10a) is wired: `needs: lint`, with `typecheck` and `test` carrying `needs: sast`; Trivy scans the built image in `docker` — see "Security" below. Coverage gate = 100% in `vitest.config.ts`; test stage fails on drop. All actions are pinned to their Node-24 majors (`checkout@v7`, `setup-node@v6`, `upload-artifact@v7`, `dorny/test-reporter@v3`, `docker/setup-buildx-action@v4`, `docker/build-push-action@v7`) ahead of the Sept 16 2026 Node 20 runner removal; the project's build/test `node-version: 20` is unchanged. The Playwright `e2e` container job marks the workspace a git `safe.directory` before `test-reporter` shells out to `git ls-files`, fixing the in-container "dubious ownership" exit-128.
- **Docker**: multi-stage `Dockerfile` (node:20-alpine → nginx:alpine), single-service `docker-compose.yml`
- **Launchers**: `bi_service.sh` / `bi_service.bat` with banner + URL block + `[k]/[q]/[v]/[r]` loop
- **Env template**: `.env.example` documents `BI_PORT`
- **`.claude/` wiring**: `SessionStart` / `PreToolUse` (blocks writes to `.env*`, `credentials.*`, `secrets.*`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, ssh keys) / `PostToolUse` (state-sensitive + renderer warnings) / `PreCompact` (preservation brief) / `Stop` (uncommitted-changes + memory review)

## Implemented Charts (193 / 193; ECharts + deck.gl)

- **distribution:** `histogram`, `box_plot`, `ecdf`, `kde`, `frequency_polygon`, `rug_plot`, `strip_plot`, `dot_plot`, `violin_plot`, `ridgeline_plot`, `beeswarm`, `joy_plot`, `qq_plot`, `pp_plot`, `probability_plot`, `cumulative_distribution_plot`, `stem_and_leaf`, `raincloud_plot`, `sina_plot`, `letter_value_plot`, `quantile_dot_plot`
- **categorical:** `bar`, `horizontal_bar`, `grouped_bar`, `stacked_bar`, `lollipop`, `pareto`, `percent_stacked_bar`, `cleveland_dot`, `dumbbell`, `slope`, `waffle`, `mosaic_plot`, `marimekko`, `pictogram`, `spine_plot`
- **composition:** `pie`, `donut`, `nested_donut`, `composition_stacked_bar`, `composition_percent_stacked_bar`, `composition_stacked_area`, `composition_percent_stacked_area`, `composition_treemap`, `composition_sunburst`, `composition_waffle`
- **time-series:** `line`, `area`, `stacked_area`, `multi_line`, `step`, `calendar_heatmap`, `streamgraph`, `percent_stacked_area`, `sparkline`, `horizon_chart`, `seasonal_subseries_plot`, `lag_plot`, `autocorrelation_plot`, `partial_autocorrelation_plot`, `run_chart`, `control_chart`, `fan_chart`, `forecast_cone`, `event_timeline`, `gantt_chart`, `swimlane_timeline`, `range_bar`
- **relationships:** `scatter`, `bubble`, `colored_scatter`, `regression_plot`, `polar_scatter`, `polar_line`, `ternary`, `loess_smoother_plot`, `faceted_scatter`, `pca_scatter`, `t_sne_plot`, `umap_plot`, `radar`, `joint_plot`, `hexbin_plot`, `two_d_density_plot`, `contour_plot`, `filled_contour`, `biplot`, `andrews_curves`, `radviz`, `parallel_coordinates`, `pair_plot`, `scatter_matrix`, `correlation_heatmap`, `covariance_heatmap`
- **matrix:** `heatmap`, `annotated_heatmap`, `clustermap`, `confusion_matrix`, `correlation_matrix`, `distance_matrix_heatmap`, `image_raster_plot` (regl), `calendar_matrix`, `tile_map`, `quilt_plot`
- **finance:** `candlestick`, `volume`, `ohlc`, `heikin_ashi`, `renko`, `kagi`, `point_and_figure`, `price_volume`, `drawdown`, `equity_curve`, `return_series_line`, `rolling_volatility_plot`, `order_book_depth_chart`, `yield_curve`, `candlestick_heatmap_by_hour_day`
- **network-flow:** `sankey`, `funnel`, `force_directed_graph`, `arc_diagram`, `chord_diagram`, `alluvial_diagram`, `waterfall_chart`, `network_graph`, `dependency_graph`, `adjacency_matrix`
- **geographic:** `point_map`, `bubble_map`, `symbol_map`, `choropleth_map`, `filled_map`, `density_map`, `hexbin_map`, `geospatial_heatmap`, `cartogram`, `flow_map`, `route_map`, `isochrone_contour_map`, `voronoi_map`, `tile_grid_map` (deck.gl)
- **3D:** `three_d_scatter`, `three_d_surface`, `three_d_wireframe`, `three_d_contour`, `three_d_bar_chart`, `three_d_volume_rendering` (deck.gl OrbitView)
- **hierarchical:** `treemap`, `sunburst`, `tree`, `icicle`, `dendrogram`, `radial_tree`, `circle_packing`, `partition_chart`
- **statistical:** `error_bar`, `residual_plot`, `actual_vs_predicted`, `roc_curve`, `pr_curve`, `calibration_curve`, `confusion_matrix_chart`, `feature_importance`, `mean_ci_band`, `forest_plot`, `bland_altman`, `lift_chart`, `gain_chart`, `survival_curve`, `cumulative_hazard_plot`, `shap_summary_beeswarm`, `shap_dependence_plot`, `partial_dependence_plot`, `ice_plot`
- **specialized:** `gauge`, `kpi_card`, `bullet_chart`, `funnel_area`, `pyramid_chart`, `population_pyramid`, `topic_term_bubble`, `cohort_retention_heatmap`, `retention_curve`, `conversion_path_chart`, `bump_chart`, `word_cloud`, `sequence_diagram`, `ranking_table_with_sparklines`, `table`, `small_multiples`, `faceted_dashboard_grid`

Shared compute: `src/data/stats/` (`quantiles`, `kernelDensity`, `buildHierarchy`, `rocCurve`, `prCurve`, `calibrationCurve`, `normalQuantile`, `qqPoints`, `isPositiveLabel`, `reduceFiniteValues`, `autocorrelation`, `partialAutocorrelation`, `survivalCurve`), `src/data/transforms/` (`groupByAggregate`), `src/charts/echarts/` (`pivotLongForm`, `finiteCategoryValues`, `histogramBins`, `matrixGrid`, `relationshipGrid`, `multivariate`, `alignedScores`, axis/grid/tooltip helpers).

## Recent Changes (v0.3.0 — Full Alignment Pass)

- Enabled TypeScript `strict: true` across `tsconfig.app.json` and `tsconfig.test.json` — zero cascading errors
- Fixed a latent bug in `src/data/shape-detector.ts`: `inferType` now checks the geo name pattern *before* numeric so `lat`/`lng`/`lon` columns with numeric values are correctly typed as `geo_point` (previously shadowed to `float`)
- Split `ChartArea.tsx` per "one concept per file": extracted `ColumnPicker` and `ChartCanvas` into their own files
- Rewrote project `CLAUDE.md` with the mandatory-re-read directive, explicit stack overrides (Zustand vs Redux, ECharts vs Chart.js, npm vs pnpm) justified against global `CLAUDE.md` §3, and refreshed structure/testing/CI sections
- `.claude/settings.json` gained `PreToolUse` secrets blocker and `PreCompact` preservation brief per global §8
- **Tests:** added 187 tests across data utilities, stores, registry, renderers, chart family implementations, theme, `lib`, and every component. Total: **191 passing.**
- **Coverage gate:** `vitest.config.ts` thresholds set to 100% on statements/branches/functions/lines. `/* v8 ignore next */` pragmas used sparingly on three genuinely unreachable branches (a string-split fallback, an empty-rows guard, and an optional-column default), each with a justifying comment.
- ESLint config extended: ignore `coverage/`, `.claude/`, respect `_`-prefix convention for unused-vars.

## Known Gaps / Caveats

- **Phase 2 chart coverage is formally complete.** All 193 catalog charts are registered, unit-covered, Gate-3-covered, pushed to `main`, and proven by GitHub Actions run #5 on `125061d5`.
- **Import normalization scope:** `detectShape` now emits `matrix`, `geo_polygons`, `survival`, and `event_log`. Import supports CSV/TSV, JSON, Excel `.xlsx`/`.xlsm`, and Parquet `.parquet`. Normalization handles common currency, thousands separators, percentages, accounting negatives, locale-style decimal/grouping punctuation, clear date-like column values, Excel Date cells, and Parquet Date/bigint values before analysis; ambiguous localized dates are intentionally preserved.
- **Remaining file-format scope:** legacy binary `.xls` is not implemented yet. Large Parquet files that exceed browser memory remain tied to the Phase 4 backend scale decision.
- **Local dev requires `npm ci` on the target OS.** `node_modules/` and `dist/` are ignored and untracked (`git ls-files node_modules` / `git ls-files dist` both return 0), so fresh clones must install dependencies locally; CI/Docker already do this with `npm ci`. `node_modules` is non-portable across OS/arch and should never be copied between machines.
- **Build still emits large vendor chunks** — app/chart-family chunks are split, but ECharts and deck.gl vendor internals still exceed Vite's 500 kB warning threshold. Further reduction would require deeper renderer-level lazy loading or ECharts-on-demand imports.
- **deck.gl cleanup is correct and exercised by real-browser paths.** `DeckGLChart`'s finalize() uses the mount-snapshot pattern, which is the *correct* React idiom (React nulls a child's imperative ref before the parent's passive cleanup runs — reading `ref.current` at unmount would yield null). The audit's "finalize leak" was re-examined and is a false positive; `tests/e2e/deckgl-switch-stress.spec.ts` now covers repeated real-GL chart changes across deck.gl map and 3D views.
- **npm vs pnpm override** (CLAUDE.md §3) — revisit in Phase 4.

## Security

### Verified state (2026-08-26)

- **Alpine base-image CVEs patched at build time.** `CVE-2026-14456` (`libcrypto3`/`libssl3`
  3.5.7-r0, HIGH, fixed 3.5.8-r0) is cleared by an `apk upgrade` layer in the runtime stage --
  measured on the base image as 2 HIGH before, 0 after. The base scanned clean two days earlier,
  so the layer exists to stop a future advisory from becoming a pipeline failure.
- **No image scan runs in this repo's CI**, so nothing here was gating; the change is
  preventive and no per-image scan result is claimed.

### Verified state (2026-08-24)

- **Semgrep: clean.** Verified locally by running this repo's own CI command against the working tree (0 findings). The invocation itself was broken before today — `semgrep ci` rejects `--severity`/`--error` and exited 2 without scanning.
- **Dependency audit: clean.** Verified with the repo's own audit command and threshold, after the override/upgrade remediation; install and build re-verified in the CI image.
- **Security headers verified delivered** — confirmed by serving the config in `nginx:alpine` and inspecting the response for `/` (0 headers before the fix, 4 after).

Open:
- ESLint security rules are reported but cannot fail the build (see fleet note below).

- Not run locally: gitleaks and Trivy are not part of any project toolchain here; both were exercised through their official images during verification, and CI runs them on every pipeline.

Requirements are documented (`CLAUDE.md` §10a `<security>`, master plan §4 "Security", gate lines on every phase) **and enforced**. Current state: `npm audit` clean (0 vulnerabilities); no `dangerouslySetInnerHTML` or outbound `fetch` in `src/`; `.env*`/secrets writes blocked by the `PreToolUse` hook; theme `localStorage` value allowlisted on read; invalid `regex` filters fail closed.

Wired:
- `sast` job in `.github/workflows/ci.yml` (`needs: lint`; `typecheck` and `test` carry `needs: sast`; `permissions: security-events: write`): CodeQL `javascript-typescript`, `pipx run semgrep scan` (`auto` + `p/owasp-top-ten` + `p/typescript` + `p/react` + `p/docker`, `--severity ERROR --error`) with SARIF upload to Security → Code scanning and a fail-on-findings step, `gitleaks/gitleaks-action@v2`, `npm audit --audit-level=high`. No `continue-on-error`.
- Trivy (`aquasecurity/trivy-action@0.28.0`, `HIGH,CRITICAL`, `exit-code: 1`, `ignore-unfixed: true`) against the image built in the `docker` job, which now builds with `load: true`.
- `eslint.config.js` extends `security.configs.recommended` + `noUnsanitized.configs.recommended` (0 errors; the `detect-object-injection` heuristic reports warnings only).
- `package.json` has a `sast` script (Semgrep + gitleaks + `npm audit`) for local parity with `/pre-commit`.
- `nginx.conf` sends CSP (`default-src 'self'`, `script-src 'self'`, `connect-src 'self'`, `object-src 'none'`, `frame-ancestors 'none'`; `style-src 'unsafe-inline'` documented as required by ECharts/Tailwind runtime styles), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`.
- **CSV formula injection closed:** `escapeCSVCell` (`src/data/export.ts`) now prefixes `'` to string cells beginning `=`, `+`, `-`, `@`, tab, or CR before quoting; numeric cells are untouched. Covered by a dedicated case in `tests/unit/data/export.test.ts`.

Still pending:
- `.semgrep/` project-rules directory.
- Remaining boundary defenses the inventory requires but that are unverified in code: prototype-pollution-safe row construction and row/byte caps in the parsers, path/control-character stripping in `exportFileName`, regex pattern-length cap (ReDoS) on the `regex` filter op.

## What's Next

Per [`docs/EXECUTION_ROADMAP.md`](EXECUTION_ROADMAP.md), M3 shipped in three verified waves plus twenty-eight wave-4 tranches, M4 slices 1–13 are shipped, M5 slices 1–6 have finished the deck.gl geographic and 3D catalog work, and the catalog-alignment pass completes Phase 2 chart coverage — **193/193**. **Gate 3 (the visual-regression harness) is in place** for all registered charts. The immediate next steps:

- **Phase 2 closeout:** complete on `main` at `125061d5`; GitHub Actions run #5 is green across lint, typecheck, test+coverage, build, visual regression, and Docker build.
- **M4 (continue):** polish/integrate the analysis loop beyond slice 13 as needed based on hands-on testing feedback.
- **Deferred to later M5 / Phase 4** (if still desired after chart-count completion): shader-file conventions, additional regl catalog migrations, true variadic-column contracts for richer future chart variants, legacy `.xls` import decisions, and backend-assisted large-dataset scale.
- Every new chart ships through the contract harness + a unit test AND the Gate-3 visual baseline; the 100% coverage gate + the e2e drift guard enforce this.
