# Business Intelligence — local env template
# Copy to .env and adjust as needed.

# Host port the nginx container is exposed on (container listens on :80 internally).
# Consumed by docker-compose.yml and bi_service.{sh,bat}.
BI_PORT=5176
